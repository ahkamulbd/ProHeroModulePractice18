// Printing Array using loop:

// var numbers = [45, 87, 89, 32, 51, 25];

// for (var i = 0; i < numbers.length; numbers++) {
//     var number = numbers[i];
//     console.log(number);
// }

// Printing Array of String using loop:

var items = ['bottle', 'mouse', 'sunglass', 'pen', 'mouse', 'keyboard', 'pen-stand'];

for (var i = 0; i < items.length; i++) {
    var item = items[i];
    console.log(item);
}